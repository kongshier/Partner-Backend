server:
  port: 8102
  servlet:
    context-path: /api
    session:
      cookie:
        domain: xxxx
        same-site: lax
        secure: false
spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://xxxxx/partner?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: shier
  # session 失效时间 30 天
  session:
    timeout: 2592000
    store-type: redis
  # redis配置
  redis:
    port: 6379
    host: 8.134.37.7
    database: 0
    password: kcsen281017
aliyun:
  oss:
    file:
      endpoint: xxxxx
      keyid: xxxxx
      keysecret: xxxxx
      bucketname: xxxx
